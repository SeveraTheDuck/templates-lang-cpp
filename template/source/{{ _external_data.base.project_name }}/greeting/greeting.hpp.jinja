{%- set ns = _external_data.base.project_name | replace('-', '_') -%}
#pragma once

#include <string>
#include <string_view>

namespace {{ ns }} {

// Returns a greeting for the given name. Replace with your own API.
[[nodiscard]] constexpr std::string Greeting(std::string_view name);

}  // namespace {{ ns }}
